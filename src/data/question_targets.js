export const QUESTION_COUNT_TARGETS = Object.freeze({
  regular: Object.freeze({
    decode: 10,
    execute: 7,
    debug: 5,
  }),
  final: Object.freeze({
    decode: 3,
    execute: 3,
    debug: 3,
  }),
});

export const FINAL_CHILD_COUNT = 3;
